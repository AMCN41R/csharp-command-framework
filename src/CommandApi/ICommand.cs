namespace CommandApi
{
    /// <summary>
    /// Represents a command object that can be handled by the command pipeline.
    /// </summary>
    public interface ICommand
    {
        // no enforced props, just for type constraints
    }
}
